import 'package:flutter/material.dart';

class theIntroScreen extends StatelessWidget {
  const theIntroScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
